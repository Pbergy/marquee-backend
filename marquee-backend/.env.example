# PostgreSQL connection string (e.g. from Supabase, Railway, or local Postgres)
DATABASE_URL="postgresql://user:password@localhost:5432/marquee"

# Generate with: openssl rand -base64 32
JWT_SECRET="replace-with-a-long-random-string"

# From your Stripe dashboard (use the test key while building)
STRIPE_SECRET_KEY="sk_test_..."

# Your frontend's URL, used for Stripe Connect onboarding redirects
APP_URL="http://localhost:3000"

PORT=4000
